<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/miko.png') }}">        
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>

        <header class="site-header" style="background-image: url({{ asset('img/forestheader.jpg') }})">
            <span class="site-title">My Symfony Site</span>
        </header>

        <nav class="navbar">
            <a href="{{ path('home') }}">Home</a> |
            <a href="{{ path('about') }}">About</a> |
            <a href="{{ path('report') }}">Report</a> |
            <a href="{{ path('lucky') }}">Lucky</a> |
            <a href="{{ path('api') }}">API</a>
        </nav>

        <main class="main">
            {% block body %}{% endblock %}
        </main>

        <footer class="footer">
            <p>Denna sidan är Copyright © av mig.</p>
        </footer>
    </body>
</html>
